# redis.conf

# Enable persistence with an RDB snapshot every 15 minutes
save 900 1
save 300 10
save 60 10000

# Set the maximum memory policy
maxmemory 512mb
maxmemory-policy allkeys-lru

# Log level
loglevel notice

# Redis password (optional, remove or change as needed)
